# タチコマ計画 ― 全記録と復元書

最終更新 2026-08-16。**このファイル1つで、プロジェクトの全体像を把握し、
評価し、ゼロから1日で復元できる**ことを目的とする。

- 日々の現在地と設計判断の正 → [CLAUDE.md](CLAUDE.md)（毎セッション読まれる）
- 環境構築と運用手順 → [README.md](README.md)
- これから作るものの受け入れ条件 → [REQUIREMENTS.md](REQUIREMENTS.md)
- 本書はそれらを束ね、**歩み・思想・評価**を1枚に集約したもの

---

## 0. 30秒サマリ

M5Stack の StackChan（ESP32-S3 デスクトップロボット）をフォークし、頭脳を
自前の Gemini ベース AI ゲートウェイに差し替えた**音声対話ロボット「タチコマ」**。
2026年1月に土台を取り込み、7〜8月に独自実装が集中。**257コミット / テスト798件
（ゲートウェイ177・注文305・PC側313・規律3）全通過**（2026-08-22 実測）。

**核心思想は「人格は1つ、入口は複数」。** 記憶・声・感情・権限を持つ1つの人格に、
実機・PCマイク・スマホ・スマートグラス（Even G2）・（開発中）会社CRM という
複数の入口からアクセスする。**AIはデバイスに載せず、頭は家のPCに1つだけ。**

いま実際に動くこと:
- StackChan実機と会話（頭タッチ / ハンズフリー、記憶・Web検索・話者識別つき）
- PCのUSBマイクで話す → タチコマの声とモーションで返る
- スマホのブラウザで話す
- **Even G2（スマートグラス）で話す**（自作アプリを正規インストール）
- Even G2で Claude Code / Codex を操作（公式ターミナルモード）
- Claude Code の実行許可を**タチコマに声で承認**（低リスク限定・安全側フェイル）
- **マクドナルドのモバイルオーダーを音声で**（店舗検索→カート構築→決済直前まで）

---

## 1. コンセプトと設計思想（これがタチコマの正体）

タチコマを「よくある音声アシスタント」と分けているのは、次の非自明な設計判断。

### 1.1 人格は1つ、入口は複数

記憶は**機体ではなく人格**に紐づく（`gateway/memory/tachikoma.json`）。当初は
機体のMAC名で保存していたが、それでは2台目が「同じ顔・同じ声・共通の過去なし」に
なる ―― 構想と正反対だった（2026-07-28 に是正）。いまはどの入口から話しても
同じ記憶・同じ人格。機体は「応答先」の単位でしかない。

### 1.2 秘密は守らせるのではなく、渡さない

聞いてはいけない事実は**そもそもモデルのコンテキストに載せない**。プロンプトで
「言うな」と指示する方式は、同情的な聞き方で引き出される余地が残る。話者を声紋で
識別し、権限（master/household/colleague/guest/unknown）に応じて記憶を出し分ける。
本人以外が聞いている場ではmaster限定の事実（名前・住所・勤務先・誕生日）は
モデルに渡らない。CRM連携でも同じ ―― 顧客データはGeminiに渡さず、答えの文だけ返す。

### 1.3 デバイスにAIを載せない

返答生成・記憶・声・話者識別はすべてPC側ゲートウェイ。APIキーはPC側のみ。
デバイスが増えても頭は1つ。この設計ゆえにPCマイク・スマホ・グラスという
入口を、実機のファームを一切変えずに増やせた。

### 1.4 機械に「順序」を推測させない

分かれた記憶の統合（`merge_memory.py`）では、会話に時刻が無いため2つの履歴を
機械が並べると「唯一信頼される場所に嘘の過去を書く」ことになる。だから
統合は人が順序を決める。自動では何もしない。

### 1.5 黙って劣化させない

声紋モデルが読めない・依存が無い等で機能が落ちるとき、1行のログではなく
理由と影響を明示する（2026-08-14、Python環境が入れ替わり声紋が無言でOFFに
なった事故の教訓）。

---

## 2. アーキテクチャ

### 2.1 全体図

```
                     家のPC（頭・常時起動＝タスクスケジューラ）
        ┌───────────────────────────────────────────────┐
        │ Tachikoma Gateway (server.py, :8080)           │
        │   /v1/transcribe  音声→テキスト＋話者識別        │
        │   /v1/chat        Gemini＋Web検索＋記憶＋感情    │
        │   /v1/speak_queue 実機が2秒ごとに取得→再生       │
        │   /v1/vision      顔検出・顔認識                 │
        │   /talk /g2/      スマホ・グラス用ページ配信      │
        │ VOICEVOX (:50021) ずんだもんTTS 約755ms/文       │
        │ notifier/         承認リレー・PCの耳             │
        │ orderagent (:xxxx) モバイルオーダー(R9)          │
        │ Even Terminal (:3456 Claude / :3457 Codex)      │
        └───────────────────────────────────────────────┘
              ▲          ▲          ▲          ▲
     ┌────────┘   ┌──────┘   ┌──────┘   ┌──────┘
  StackChan実機  PCのUSBマイク  スマホ    Even G2＋iPhone
  (家/会社)       (机の上)      /talk     (BLE中継)
```

### 2.2 2つのリポジトリ

| 場所 | 中身 |
|---|---|
| `firmware/` | 実機ファーム（ESP32・ESP-IDF v5.5.4）と Tachikoma Gateway（Python） |
| `notifier/` | PC側の通知・承認基盤。元は別リポジトリ `Tachikoma`、2026-07-30に `git subtree` で履歴ごと統合 |
| `orderagent/` | R9モバイルオーダー（Playwright + SQLite） |
| `glasses/` | Even G2 アプリ（TypeScript + Vite + Even Hub SDK） |
| `server/` | 自前サーバ（M5Stack公式アプリの代替。カメラ映像をブラウザへ） |

### 2.3 状態機械（実機の背骨）

```
TachikomaState    : Booting, Idle, Listening, Thinking, Speaking, Reacting, Error, Sleeping
TachikomaReaction : None, Happy, Confused  (状態ではなくリアクション。Reactingの上に乗る)
```

感情タグは Gemini の返答先頭 `[happy]/[sad]/[neutral]` → 剥がして
`X-Tachikoma-Emotion` ヘッダで実機へ → 音声と同時にモーション再生。
タグは音声より先に決まるので動きが声に遅れない。

---

## 3. 全機能の現状（テスト済み・実機確認済み）

| 機能 | 状態 | 根拠 |
|---|---|---|
| 実機の会話（頭タッチ） | ✅ 実用 | 応答3.7秒。記憶・Web検索・話者識別 |
| ハンズフリー会話 | ✅ 実機確認 | 2週間の難物。原因5層を特定・修正（§5参照） |
| 感情表現 | ✅ 端から端まで | 判定→ヘッダ→モーション。実機ログで裏付け |
| 権限システム | ✅ ゲートウェイ側完了 | 声紋・顔・「ここだけの話」で秘密指定 |
| 記憶の人格化 | ✅ 稼働 | 5入口で共通の記憶 |
| PCの耳（USBマイク） | ✅ 実機確認 | `notifier/pc_ear.py`。実機ファーム無改造 |
| スマホ入口 | ✅ 稼働 | `/talk`。tailnet HTTPS |
| Even G2 アプリ | ✅ 正規インストール・会話成立 | `glasses/`。`/g2/` から配信、`.ehpk`で導入 |
| Even Terminal（Claude/Codex） | ✅ 会話成立 | 公式機能。3456/3457で常駐 |
| 承認リレー | ✅ コード完成・フック登録済み | 実地5分テストのみ残 |
| モバイルオーダー(R9) | ✅ 決済直前まで確認 | マック。決済段はサイト側障害で保留（§8） |
| 自前サーバ認証突破 | ✅ 稼働実績 | M5Stackの非公開RSA認証を自前実装 |

自動起動タスク（ログオン時）: Tachikoma Gateway / VOICEVOX Engine /
Even Terminal / orderagent。**PCを立ち上げれば全部揃う。**

---

## 4. これまでの歩み（時系列）

| 時期 | 何を |
|---|---|
| 2026-01〜07-02 | M5Stack公式StackChanを取り込み、ハードウェア制御を安定化 |
| 07-19〜21 | アバター基盤・状態機械・AIゲートウェイ基盤。「頭を押して話す→答える」配線完成 |
| 07-22〜26 | 実運用ハードニング。実機バグ4件（再起動ループ・タッチクラッシュ・スタックオーバーフロー・工場出荷クラウド認証の無効化）。録音捕捉率 21%→ほぼ100% |
| 07-26 | 感情表現・話者識別・顔認識・記憶。開発レポート作成 |
| 07-28（会社） | **M5Stackの非公開アプリ認証を自前実装で突破**。自前サーバ＋MariaDB構築。勝手に喋る問題を根治 |
| 07-28（家） | 記憶を機体ごと→人格ごとへ。別拠点の転送役・記憶統合ツール |
| 07-29 | 会社でコード取り込み・push |
| 07-30 | コード整理（ルーティングを表に・重複解消）。要件定義書R1-R8。2リポジトリ統合。README刷新 |
| 08-13 | **ハンズフリー修復（§5）**。要件着手 |
| 08-14 | Python入れ替わりで声紋が無言OFFになった事故を修正。声紋モデルの温めを完全化 |
| 08-15 | R1・R2クローズ。R4承認リレー配線。R5スマホ入口＋Even Hub登録。声紋の自動学習 |
| 08-16 | **R5完成（G2にタチコマ搭載）**。Codex接続。R9モバイルオーダー実装・ライブ検証 |

---

## 5. 高くついた教訓（同じ轍を踏まないために）

ハンズフリーが「2週間動かなかった」のは**5つの原因が積み重なっていた**から。
それぞれ単独では同じ「動かない」症状にしかならず、順に剥がすしかなかった。

1. **vendoredライブラリの意図的クラッシュ** `*(int*)0=0;` ―― 返事直後の
   マイク再オープンでI2S再設定が失敗するとpanic。「1回目だけ動く」の正体。
   `firmware/patches/apply_codec_dev_fix.py` で再適用（managed_componentsはgit外）
2. **無音判定の時計停止** ―― 録音開始で早期returnし、発話長が常に0ms→全部破棄
3. **閾値が部屋の暗騒音より下**（1400/900 vs 暗騒音1457-2257）→30秒上限まで録音
4. **PATHのpython入れ替わり**（3.9がtorch無し）→声紋が無言OFF
5. **温めがモデルだけで経路が冷えたまま**（librosa→numba初回コンパイル14.9s）
   →最初の話者がrequest内で67秒待たされ、実機が諦める

**教訓（普遍）**:
- ログを絞るとき `boot:` を捨てない（再起動の証拠を2週間捨て続けた）
- 「たまたま動いた」検証はプロセスの寿命を確認する
- 閾値は必ず実測してから置く（推測値が暗騒音より下だった）
- 探し物が見つからないときは、まず**探した範囲**を疑う（承認基盤は別リポジトリにあった）

---

## 6. ゼロからの復元手順（1日で戻す）

前提: Windows PC、Python 3.11、Node、Git、M5Stack CoreS3実機、
Google AI Studio APIキー。詳細は [README.md](README.md) 1章。

```powershell
# 1. 取得
git clone -b phase4-ai-gateway https://github.com/Daisuke-0409/StackChan.git
cd StackChan

# 2. SSLで落ちるなら winroots.pem を作る（README 1.3）
# 3. 依存
python -m pip install torch librosa opencv-python numpy scipy sounddevice
# 4. gateway\.env を作る（DEVICE_TOKEN, AI_PROVIDER_API_KEY ほか。README 1.5）
copy firmware\gateway\.env.example firmware\gateway\.env
# 5. 声紋・顔モデル取得（54MB）
python firmware\docs\fetch_models.py
# 6. VOICEVOX 起動（任意・声が速くなる）
# 7. コーデックのクラッシュ修正を当てる（実機を焼くなら必須）
python firmware\patches\apply_codec_dev_fix.py
# 8. 実機に焼く（バックアップ→ビルド→flash。README 3章）
#    ESP-IDF export は VS Code 拡張レイアウトのため4行の環境変数が要る（README 1.8）
# 9. ゲートウェイ起動
powershell -File firmware\gateway\run_gateway.ps1
```

テストで健全性を確認:
```powershell
powershell -File run_tests.ps1
```
全スイート（ゲートウェイ／注文エージェント／PC側／リポジトリ規律）が走り、件数と
合否が出る。3スイートとも標準ライブラリだけで通るので、同じものが CI でも走る
（`.github/workflows/tests.yml`）。

git に入らないもの（別途用意）: `.env` / `memory/`（個人データ・声紋）/
`models/` / `.env.local`（グラスのトークン）/ サーバの秘密鍵・config.yaml。

---

## 7. 最適化したロードマップ

当初のPhase番号は文書間で衝突したため**廃止し、名前と順序で管理**。
現状を反映して再構成したものが以下。

### 完了

機体と基盤 / 会話ループ / 実運用ハードニング / 感情表現 / 権限システム /
自前サーバ認証 / 記憶の人格化 / 別拠点の接続（実装） / **R1ハンズフリー** /
**R2 PCの耳** / **R4承認リレー（コード）** / **R5 Even G2** / Codex接続 /
**R9モバイルオーダー（決済直前まで）**

### 次（すぐ効く・小さい）

| 項目 | 内容 | 残り |
|---|---|---|
| R4実地テスト | デーモン起動済み。音声承認を1回試すだけ | 5分 |
| R3 持ち出し中継 | ルーターのLANを192.168.2.x化＋中継機に固定IP。コードは完成 | 1晩＋外出 |
| R9決済の復活確認 | マックWeb版決済がサイト側障害。素Chromeで再テスト、駄目なら他チェーン | 調査 |
| メガネの磨き | 声紋サンプル追加 / レンズに承認サマリ表示 | 各1晩 |

### その後（機材・会社が要る）

| 項目 | 内容 | 前提 |
|---|---|---|
| R6 CRM連携 | 会社PCで照会役、答えの文だけ返す（顧客データはGeminiに渡さない） | 会社訪問1回 |
| R8 Mac mini + NAS | 頭を常時起動機へ移し記憶をDB化。「PC無しでも喋る」達成 | 機材 |
| R7 実機HTTPS+ペアリング | 通信暗号化・機体ごとの鍵。会社に機体を置く直前 | Mac mini後が得 |
| 社内リリース | 上記が揃ってから | ― |

---

## 8. 既知の問題・技術的負債

- **マックWeb版決済がエラー**（2026-08-16）。自動化・素Chrome・iPhone実機LTEの
  全条件で再現し、サイト側障害または Web版決済廃止と確定。R9の決済段はここ待ち
- **メガネからだと声紋が届きにくく他人扱い**になりやすい（マイク特性差）。
  master限定の記憶が伏せられる。故障ではない。R7のペアリング設計と一体で決める
- **プライベートビルドがメガネのホームに並ばない**（現行アプリの制約）。
  iPhoneの「Me→開く」から起動する運用。コミュニティ製ランチャー(even-menu)も候補
- **タスク急死**の正体は「デスクトップの窓を閉じる癖」で conhost 巻き添え。
  コード根治は不要、窓を残さない運用で対応
- 顔追従の首振りは停止中（タッチセンサが可動部にあり誤検知するため。押下と振動を
  区別できるまで戻さない）
- vendored コードに他の abort 地雷が数件（通常運用では踏まない・記録済み）

---

## 9. リポジトリ地図（主要ファイル → 役割）

| ファイル | 役割 |
|---|---|
| `firmware/gateway/server.py` | ゲートウェイ本体（全エンドポイント） |
| `firmware/gateway/people.py` `biometrics.py` | 話者識別・声紋/顔の埋め込み |
| `firmware/gateway/merge_memory.py` | 分かれた記憶の統合（人が順序を決める） |
| `firmware/gateway/forwarder.py` | 別拠点の中継（会社の体→家の頭） |
| `firmware/gateway/order_bridge.py` | R9をゲートウェイに繋ぐ |
| `firmware/gateway/webui.py` | 設定ページ・/talk ページ |
| `firmware/main/stackchan/voice_input/voice_input_controller.cpp` | 録音・ハンズフリー（§5の主戦場） |
| `firmware/main/ai_gateway/speech_announcer.cpp` | 音声取得・再生・感情モーション |
| `firmware/main/stackchan/state/` | 状態機械 |
| `firmware/patches/apply_codec_dev_fix.py` | vendoredクラッシュ修正の再適用 |
| `notifier/tachikoma_notifier/pc_ear.py` | PCのUSBマイク入口 |
| `notifier/tachikoma_notifier/approval_daemon.py` | 承認リレーのデーモン |
| `notifier/tachikoma_notifier/claude_hook_approval.py` | Claude Codeフッククライアント |
| `orderagent/` | モバイルオーダー（intent/store/cart/payment guard） |
| `glasses/src/main.ts` | Even G2 アプリ本体 |

---

## 10. 評価者向けサマリ

**何を作ったか**: 市販のデスクトップロボットを、記憶と人格を持ち、複数の入口
（実機・PC・スマホ・スマートグラス）から同一人格でアクセスでき、
権限に応じて秘密を守り分け、開発エージェントの承認や実世界の注文までこなす
パーソナルAIコンパニオンに作り替えた個人プロジェクト。

**技術的な見どころ**:
- 非公開バイナリの認証（RSA-OAEP）をドキュメントとソースから再構成し自前実装で突破
- 「モデルに渡さない」に徹した権限設計 ―― 声紋で相手を識別し記憶を出し分ける
- 実機ファーム無改造で入口を増やせる、頭を1つに集約したアーキテクチャ
- 5層に積み重なった実機バグを、勘ではなく毎回実機ログの数値から特定して解決
- vendoredライブラリの意図的クラッシュを発見し、消えても復元できる形で修正

**プロジェクトの規模**: 257コミット / テスト798件全通過 / 実機・PC・スマホ・
グラスの4系統で稼働確認済み。

**設計の一貫性**: 全体を貫く原則（人格は1つ／秘密は渡さない／機械に順序を
推測させない／黙って劣化させない）が、コミットメッセージとコードコメントに
理由まで含めて記録されている。ドキュメント（CLAUDE.md / README / REQUIREMENTS /
本書）が実装と同じコミットで更新され、14日空いても1日で復帰できる状態を保っている。

---

*このファイルは実装と同じリポジトリにある。最新の日々の状態は必ず CLAUDE.md を
併読すること（本書は節目でまとめる総目録、CLAUDE.md は毎セッションの現在地）。*
