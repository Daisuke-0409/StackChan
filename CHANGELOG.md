# CHANGELOG

タチコマの版と、その版で何ができるようになったか。細かい変更は git log が正。
ここは**「どの版まで戻せば何が動くか」**を判断するための粒度で書く。

版のつけ方: `0.x.y`。**1.0 は決済を本番でONにして事故なく回った日**にする。
それまでは全部 0.x = 「動くが、まだリリースしていない」。

現在の版は [VERSION](VERSION)。走っている版は各サービスが名乗る:

    curl http://127.0.0.1:8080/health    # gateway
    curl http://127.0.0.1:8766/health    # order agent

---

## 0.9.0 — 2026-08-22 「リリース前の総点検」

開発会社5部門ぶんのエージェントで自己監査し（[監査結果](docs/RELEASE_AUDIT_2026-08-22.md)）、
出てきた致命傷を潰した版。**機能追加はゼロ、壊れにくさだけを上げた版**。

安全:
- 決済の二重実行を塞いだ（承認をロック下で1回だけ受け取る）
- 決済後の判定を `sleep(8)+1回読み` から60秒ポーリングへ。
  「遅い成功」を失敗と誤判定して再注文を誘う経路を消した
- クラッシュしたジョブを起動時に回収（決済中に落ちた注文は必ず人に報告される）
- 状態遷移を全て state machine 経由に。存在しない遷移は起きずに例外になる
- メガネに専用トークン（マスタートークンの配布をやめた）
- トークン総当たりに時間コスト（gateway / CRMリレー）
- CRMリレーのトークン比較を constant-time に（`!=` はタイミングで漏れる）

運用:
- `self_check.ps1` と5分毎の監視タスク（死んだサービスを自動復旧）
- ゲートウェイに二重起動ガード（「listeningなのに無言」の正体）
- 全launcherが .env を UTF-8 で読む（日本語設定の文字化けを撤去）
- `run_tests.ps1` で全795件が1コマンド。GitHub Actions でも走る
- **notifier の313件が discover から見えていなかったのを修正**（`__init__.py` 欠落）

思想と再現性:
- [PHILOSOPHY.md](PHILOSOPHY.md) / [SOUL.md](SOUL.md) / [OPERATIONS.md](OPERATIONS.md) に四層分離
- [RESURRECTION.md](RESURRECTION.md) と `requirements/` で半日再建の手順
- デスクトップの「タチコマseed」= 出荷時原本

**この版で決済はまだOFF**（`ORDER_PAYMENT_ENABLED` 未設定がデフォルト）。

---

## 0.8 — 2026-08-21 「耳をローカルに」

- 音声認識をクラウドから faster-whisper (kotoba-whisper) へ。2.4〜2.8秒、固有名詞に強い
- 会社の機体を復活（DEVICE_TOKEN の複数持ち）
- PCを静的IP 192.168.2.120 に固定（DHCP変化で全機体が無言になった事故の対策）

## 0.7 — 2026-08-19 「モバイルオーダー」

- スタバ／モスのアダプタ（実店舗のカートまで実地で到達）
- 決済ゲート `payment.py`（金を動かす唯一のファイル、既定はドライラン、上限3000円、
  結果は PAID / NOT_PLACED / UNKNOWN の三値、リトライ経路なし）
- マクドナルドはゲスト決済が閉じていた（一次証拠つきで MENU_ONLY に降格）
- 通称からの推測（「ダブチ」「アイスコーヒー」）と、読み上げでの解釈提示

## 0.6 — 2026-07-30 「会社の台帳と繋がる」

- CRM連携（ゲートウェイ ↔ 会社PCのリレー、Tailscale越し）
- 墓所・顧客の照会と2ターン絞り込み。顧客データはGeminiに渡さない
- 記憶を機体ではなく人格に紐づけ（1つの人格・複数の入口）
- 権限システム（master限定の記憶。声紋が外れたら話さない）

## 0.5 — 2026-07-2x 「声で対話できる体」

- 頭タッチで push-to-talk、/v1/transcribe、返答の読み上げ、感情モーション
- VOICEVOX（ずんだもん）へ移行
- Phase 2-4 の土台（モーション・状態機械・AIゲートウェイ）

## 0.1 — 2026-01 〜 「フォーク」

- M5Stack StackChan (CoreS3 / ESP32-S3) を土台に取り込み、xiaozhi のクラウドを完全停止

---

## 戻し方（ロールバック）

    git tag                       # 版の一覧
    git checkout v0.9.0           # その版のコードに戻す
    powershell -File self_check.ps1 -Repair   # サービスを新しいコードで上げ直す

**git に入っていないものは戻らない**（戻す必要もない）:

- `.env` — 秘密。[RESURRECTION.md](RESURRECTION.md) §0 のバックアップから
- `firmware/gateway/memory/` — 会話・人物・声紋。個人データ
- `orderagent/data/` — 注文履歴DB・ブラウザプロファイル（決済手段はここに手で登録）

つまりロールバックで失うのはコードだけで、記憶と秘密はそのまま残る。
逆に言えば、**記憶と秘密は git 以外の場所で守る必要がある**。
