# タチコマ 要件定義書

作成: 2026-08-13。エージェントが読んで実装を進めるための文書。
**現在地の正は CLAUDE.md**。本書は「これから何を・どの順で・どこまでやったら完成か」を定める。
矛盾があれば CLAUDE.md とコードを優先し、本書を更新すること。

---

## 0. コンセプト（不変）

**人格は1つ、入口は複数。**
タチコマという1つの人格（記憶・権限・声・感情）に対し、StackChan実機・PC・
Even G2・会社システムなど複数の入口からアクセスする。

- **デバイスにAIは載せない。** 頭は家のPCのゲートウェイ1つ。APIキーはPC側のみ
- **秘密は守らせるのではなく渡さない。** 聞いてはいけない事実はモデルのコンテキストに載せない
- 記憶は人格に紐づく。機体は応答先の単位でしかない

## 0.1 エージェント運用ルール（全要件共通）

1. **実機flash・git push・シークレットの扱いは、実行前に必ず人間に確認する**（CLAUDE.md の規則）
2. flash前に16MBフルバックアップ（460800 baud、16,777,216バイトちょうどを検証）
3. 会社PCでゲートウェイを起動しない（記憶が枝分かれする）
4. 修正は「観測→仮説→修正→実機ログで検証」のループで進める。勘で直さない
5. 各要件の**受け入れ条件を満たすまで完了と報告しない**。実機確認が必要なものは
   「人間確認待ち」で止まり、結果を待つ
6. ドキュメント（CLAUDE.md / README.md / 本書）は実装と同じコミットで更新する

## 0.2 検証環境

- ゲートウェイログ: `C:\Users\mylit\StackChanDev\logs\gateway.log`（自動起動タスクが追記）
- 実機シリアル: `capture_boot.py` 方式（dtr/rts=False で開く。**実行中0バイトに見えるのは正常**、終了時に書き出される）
- 自動起動タスク: `Tachikoma Gateway` / `VOICEVOX Engine (Tachikoma)`（ともにログオン時）
- テスト: リポジトリ直下で `powershell -ExecutionPolicy Bypass -File run_tests.ps1`（全スイート・件数も表示）
  個別に走らせたいときの呼び方は run_tests.ps1 の中に書いてある。
  **件数をドキュメントに書き写さない**: 数えるのは runner の仕事で、
  書き写した数は必ず古くなる（実際268/313/154/364/450と食い違っていた）

---

## 要件一覧（優先順）

| ID | 名前 | 種別 | 実機flash | 人間確認 |
|----|------|------|----------|---------|
| R1 | ハンズフリー会話の修復 | バグ修正 | 要 | 要（発話テスト） |
| R2 | PCの耳（USBマイク入口） | 新規 | 不要 | 要（発話テスト） |
| R3 | 持ち出し中継（モバイル） | 構成 | 不要 | 要（外出テスト） |
| R4 | 承認リレーの配線 | 統合 | 不要 | 要（承認テスト） |
| R5 | Even G2 連携 | 新規 | 不要 | 要（実機G2） |
| R6 | CRM連携 | 新規 | 不要 | 要（会社作業含む） |
| R7 | 実機HTTPS + ペアリング | 基盤 | 要 | 要 |
| R8 | Mac mini + NAS 移行 | 基盤 | 不要 | 要（機材到着後） |

---

## R1. ハンズフリー会話の修復 【最優先・継続案件】

**現状**: 頭タッチの1回目は動く。返事のあと頭を触らずに話す2回目以降が動かない。
7/30深夜に4つの原因を特定・修正・flash済みだが未解決。経緯と次の一手は
CLAUDE.md「2回目の発話が通らない」節に全て記録済み。

**確定している事実**:
- マイク・声量は正常（73文字認識できた実績）
- `UserSpeechStarted` をハンズフリー経路が撃たない問題は修正済み（`abd92fc`）
- 最後の観測では `follow-up speech detected` が**1件も出ない**
  （自分の声ガードが効きすぎている可能性）

**次の一手（記録済みの仮説から）**:
1. 状態機械がどの状態で止まっているかを観測する
   （`GetCurrentState()` の定期ログなど一時計装を入れてよい。ただし
   `TACHIKOMA_DEBUG_TIMING` の教訓: **調査が終わったら必ず外す**）
2. `follow-up speech detected` が「出ない」のか「出ても弾かれる」のかを切り分ける
3. 人の声のRMS実測値を取ってから `kFollowUpSpeechRms` / `kPostSpeechCooldownMs` を調整
   （**閾値調整は原因特定の後。順序を逆にしない**）

**受け入れ条件**:
- [x] 頭タッチで1回話す → 返事の後、頭を触らずに3回連続で発話し、3回とも返事が返る
      (2026-08-15 大輔が実機で確認「ハンズフリー完成」)
- [x] タチコマの返事に反応して自分で喋り出す事象が10分間発生しない
      (自声ガードを窓の開口点で武装、実機確認済み)
- [x] 上記を実機ログで裏付ける (原因5件の特定がすべて実機ログ起点。CLAUDE.md 参照)

**達成 2026-08-15。** 先送りの磨き3件 (文間ギャップ>1500msの隙間・期限切れ競合・
極短返事で窓が開かない) は CLAUDE.md「先送り3件」に記録済み。

## R2. PCの耳 — USBマイクを入口にする 【実機を焼かない・単独で完結】

**目的**: 家ではUSBマイクの良い収音で聞き、返事は**タチコマの口とモーション**で返す。

**設計（調査済み）**:
```
USBマイク → PC録音（無音区切り） → POST /v1/transcribe（既存・話者識別つき）
→ POST /v1/chat（device_id=80456B4DE03C = タチコマ実機のID）
→ 返事はタチコマの speak_queue に載る → タチコマが喋る・動く
```
- 置き場所: `notifier/tachikoma_notifier/` に新モジュール
  （`conversation_pipeline.py` が「音声→テキストの境界は意図的に外に置いた」と
  明記している、その空白を埋めるもの）
- 録音は `sounddevice` 等の追加依存を許可（notifier初の外部依存になるため README に明記）
- **device_id をタチコマ実機と同じにする**ことで記憶・権限・感情がそのまま効く
- ハンズフリーの発話区切りロジック（RMS閾値・無音判定）はゲートウェイ側の
  定数を参考にするが、PC側で独立に持ってよい（マイク特性が違うため）

**受け入れ条件**:
- [ ] USBマイクに話しかけると、タチコマ実機が声とモーションで返事する
- [ ] PCの入口で話した内容を、後で頭タッチの会話でタチコマが覚えている（記憶共有の確認）
- [ ] 話者識別が効いている（ログに speaker 判定が出る）
- [ ] タチコマの返事中にPCマイクが自分の声を拾って再トリガーしない
- [ ] 実機ファームの変更なし

## R3. 持ち出し中継 【構成作業・コード変更は原則なし】

**目的**: タチコマ単体を外に持ち出し、モバイルルーター経由で家の頭と会話する。

**設計（決定済み）**:
```
タチコマ →(近距離WiFi)→ 中継機(forwarder.py) →(LTE+Tailscale)→ 家のゲートウェイ
```
- 実機は焼き直さない。**モバイルルーターのLANを 192.168.2.0/24 に設定し、
  中継機に 192.168.2.120 を固定割当**することで、実機は家に居ると思ったまま繋がる
- 中継機は Tailscale 導入済みの端末（ノートPC等）。`forwarder.py` は標準ライブラリのみ
- Funnel（実機から直接公開HTTPS）は**採用しない**: 実機の空きSRAM（最低14KB）では
  2秒ごとのTLSポーリングが成立しない。R7完了後に再検討

**受け入れ条件**:
- [ ] 家のWiFiが届かない場所で、頭タッチの会話が一往復成立する
- [ ] 家に戻って接続先を変えずにそのまま動く（設定の切り替え作業ゼロ）
- [ ] 手順が README に載っている（ルーター設定含む）

## R4. 承認リレーの配線 【土台は実装済み・残るは配線のみ】

**現状**: `notifier/` に approval_store / permission_relay / voice_approval_gate が
実装済み・268テスト通過。**Claude Code の実フローに繋がっていない**だけ。

**やること**:
1. Claude Code の PreToolUse / PermissionRequest hook から notifier へイベントを流す
2. notifier がタチコマに読み上げさせる（`/v1/speak` 経由・実動確認済みの経路）
3. 「はい/いいえ」の音声を STT → `voice_approval_gate` の判定へ
4. 判定結果を Claude Code へ返す（hookの応答として）
- 音声承認の対象は voice_approval_gate の既存方針を維持
  （**低リスク・読み取り専用ツールに限定**。拡大しない）

**受け入れ条件**:
- [ ] Claude Code が許可を求めると、タチコマが声で読み上げる
- [ ] 「はい」で当該ツールが実行され、「いいえ」で拒否される
- [ ] 音声で承認できるのは低リスクツールのみ（それ以外は読み上げのみで、PC側で手動承認）
- [ ] R1完了が前提（承認は短い発話の連続のため）

## R5. Even G2 連携 【調査済み・R2の後が効率的】

**前提（調査済み・CLAUDE.md「Even G2 の前提」）**:
- G2アプリ = このゲートウェイがホストするWebページ（Even Hub経由、iPhoneのEven AppがBLE中継）
- G2にスピーカー無し・カメラ無し・マイクあり → **声で話しかけて文字で返る**入口
- HTTPS必須 → 済み（tailscale serve 稼働中）。iPhoneはtailnet在籍済み

**やること**:
1. Even Hub の開発者登録、サンプルアプリを表示するところまで（人間のiPhone操作が必要）
2. ゲートウェイに G2 向けページを追加: 発話（G2マイク→STT）→ /v1/chat → **返事をテキスト表示**
3. device_id は共有人格の入口として扱う（記憶は共通、ただし応答先はG2の画面）
4. R4完了後: 承認をG2の視界に出し、R1リングで承認できる形を追加

**受け入れ条件**:
- [ ] G2をかけて話しかけると、返事が視界に文字で出る
- [ ] その会話をタチコマ実機が覚えている（人格共有の確認）
- [ ] 家の外（モバイル回線のiPhone経由）でも動く

## R6. CRM連携 【方式決定済み・会社作業を含む】

**方式（決定済み・変更禁止の線引き）**:
- 会社PC側に照会役を置き、CRM を引いて**答えの文だけ**を返す。
  顧客データ本体は会社の外に出さない
- **顧客データを Gemini に渡さない。** 定型文の組み立てで返す（AI不要の設計）
- 照会と答えは**記憶に残さない**扱いにする
- 入口: `/api/tachikoma/lookup`（＋ `find` / `show`）。契約は
  [CRM_LOOKUP_API_CONTRACT.md](docs/CRM_LOOKUP_API_CONTRACT.md)

**当初の想定から変わった2点**（2026-08-24 に本文を修正）:

1. **「ローカルは認証不要」は成り立たない。**`is_local()` は個人PINログインの
   導入（2026-08-07）で使われなくなり、いまは `require_auth()` が `/api/` 配下
   すべてにセッションを要求する。**127.0.0.1 からでも 401**。よって専用の
   `X-Tachikoma-Token` を持つ
2. **入口は `/api/field/search` ではない。**そちらは 401 を返す。CRM 側が
   タチコマ専用に `/api/tachikoma/lookup` を実装した（`5c681d2`・本番稼働中）

**受け入れ条件**（2026-08-24、会社PCから実CRMに対して確認・すべて達成）:

- [x] タチコマに「〇〇さんの墓所どこ？」と聞くと、霊園名・地区を声で返す
      — 実CRMで確認。24件ヒットする姓で `count=24` / 返却3件、実物の行を
      読み上げの組み立てに通して95文字の文になることまで確認した。
      **区画番号は `cemetery_name` の中**にあり、`area` は地区。
      `area` が空の行と入っている行が両方含まれていたので、
      「{area}地区の{霊園}」と「地区が無くても欠落を言わない」の両方を踏んでいる。
      （**2ターン絞り込みの実地確認は 2026-08-20**。今回は再確認していない。
      固定しているのは `test_a_crowded_surname_asks_for_the_given_name` ほか）
- [x] ゲートウェイのログ・記憶ファイルに顧客情報が残らない
      — `test_crm_reply_short_circuits_chat_without_memory`（`_remember_exchange`
      が呼ばれないことを検証）。照会役のログに照会した名前が出ないことも実地で確認
- [x] Gemini へのリクエストボディに顧客データが含まれない
      — 同テストで `_provider_response` が呼ばれないことを検証。**渡さないのではなく
      到達しない**ので、ログを見るより強い保証
- [x] CRM側が落ちていても通常会話は無傷
      — `test_a_dead_crm_leaves_ordinary_conversation_alone`（呼ばれたら失敗する
      fetch を渡し、雑談が素通りすることを検証）。会社PCが落ちている夜間は
      これが通常状態なので、故障ではなく前提

**再確認のしかた**（会社PCでのみ通る。値は表示されない）:

```powershell
python firmware/gateway/check_crm_live.py
```

17項目を実CRMに対して確かめる。**照会1回につき CRM の監査ログに1行残る**ので、
`asked_by` には人の名前ではなく `acceptance-check` を渡している。
CRM を NAS へ移したときは、これがそのまま移設の確認になる
（変わるのは接続先ホストだけのはず、という契約の主張を試せる）。

## R7. 実機HTTPS + デバイスペアリング 【会社に実機を置きっぱなしにする前提条件】

- ESP32→ゲートウェイの通信をHTTPS化（証明書の扱いをファームに実装）
- デバイストークンを機体ごとに分離（現状は `_authorized()` の単一文字列比較）
- 機体の登録・無効化をWeb UIから
- **メモリ制約に注意**: 空きSRAM最低14KB の実測がある。TLSセッションの持ち方は
  ポーリングごとの張り直しではなくコネクション維持を検討

**受け入れ条件**:
- [ ] 平文HTTPの経路が消える（DEVELOPMENT_BUILD警告の解消）
- [ ] 機体Aのトークンを無効化しても機体Bが動き続ける
- [ ] メモリ逼迫でクラッシュしない（24時間連続稼働）

## R8. Mac mini + NAS 移行 【機材待ち・設計のみ先行可】

**決定済みの役割分担**: Mac mini M4 Pro 32GB = 常時起動の頭 / NAS 8TB = 記憶のDB。
**NASに声紋・顔認識をやらせない**（CPU不足）。

- 記憶をJSONファイルからDBへ（これで「ゲートウェイは1台だけ」制約が消える）
- 何をどこまで残すかのデータポリシーを人間と決める（8TBは広大。音声・フレームを残すか）
- 移行時: `merge_memory.py` の思想を踏襲（機械が順序を推測しない）

**大輔の明示要件 (2026-08-15)**: 「マイクが繋がっていないときや、PCが起動して
いなくても話せるように」。前半は既に満たされている (PCの耳は追加の入口で、
無くても実機の頭タッチ・ハンズフリーは動く)。後半がこの要件の本体 —
**頭が家のPCの起動状態に依存しない**こと。Mac mini を常時起動の頭にした時点で
達成される。

**受け入れ条件** (上記を反映):
- [ ] 家のPCが完全に停止していても、実機との会話が成立する
- [ ] USBマイク・PCの耳の有無が、実機単体の会話に一切影響しない (現状すでに真)
- その他は機材到着後に詳細化

---

## R9. モバイルオーダー（音声注文→承認→事前決済）【2026-08-16 追加】

要件定義書 v1.0（大輔作成）を実装。**構造はゲートウェイと分離した別プロセス
`orderagent/`（127.0.0.1:8766）**。ゲートウェイ側は order_bridge（stdlib のみ、
エージェント停止時は完全に素通り）+ /v1/location + /v1/announce の最小追加。

**実装済み（2026-08-16、全段ライブ検証済み）**:
- Phase 0: iPhoneショートカット → POST /v1/location（n8n不要版）
- Phase 1a: 店舗検索 = `map.mcdonalds.co.jp/api/poi`（全国2129店、キー=店舗番号、
  **Google Places不要・APIキー不要**）+ ハーバサイン近傍3件 + mopEnabled確認
  + 時間帯パターン混雑推定（推定であることをログに明記）
- Phase 1b: Playwright カート構築（モバイルUA必須 / 店舗お知らせモーダル自動クローズ /
  **SPA内はpushState+popstateで遷移**（page.gotoはカートが消える）/
  明細は重複レンダリングされた2つのULの片方だけ読む / ジョブ開始時に全て削除）
- Phase 1c: 承認フロー（読み上げはスクレイプした**カート実物**から生成、
  承認は「注文」を含む明確フレーズのみ・はい単独では通らない・5分で失効）
  + 決済ゲート（明細・合計の完全突合、3,000円上限、リトライなし、CAPTCHA即停止）

**決済の現状**: `ORDER_PAYMENT_ENABLED` 未設定 = 全ジョブがドライラン。
実クリック部は**意図的に未実装**（実サイトの決済ページを大輔と一緒に
ウォークスルーしてから実装する。盲目実装は禁止）。フラグを立てても
PaymentRefused で拒否される実装になっている。

### 再設計 (2026-08-19 着手)

R9 を「マック専用の注文自動化」から「複数チェーンへ拡張可能な会話型フードオーダー
エージェント」へ再設計中。仕様は大輔作成の再設計仕様書、現在地は
`docs/R9_REDESIGN_NOTES.md`。**STEP 1〜11 のうち実サイトを要さない部分は実装済み
(242テスト通過)、既存ファイルは未変更。**残るのは Adapter の実接続 (STEP 7)、
証拠取得 (STEP 11)、2社目 (STEP 12)、クーポン (STEP 13) で、すべて実サイトが要る。

**受け入れ条件**:
- [x] 「マックで◯◯を注文して」で店舗選択→カート構築→読み上げまで自動（ライブ確認済み）
- [x] 複数商品（と区切り）・数量（2個）・受け取り方法の抽出（ライブ確認済み）
- [x] 承認語で dry_run_done、拒否語でキャンセル、無応答5分で失効
- [x] 突合検証: カート明細と承認内容の不一致は決済拒否（ユニットテスト24件）
- [ ] 実決済1件成功（少額）— **決済手段の手動登録＋共同ウォークスルー後**
- [ ] クーポン（Phase 1.5）、モス/ケンタ/スタバ（Phase 2-3、スタバはプリペイドで最安全）

---

## 付録: 明示的にやらないこと

- 顔追従の首振り再開（タッチセンサで押下と振動を区別できるまで）
- NAS共有フォルダへの複数ゲートウェイ書き込み（DB化まで禁止）
- ウェイクワード「タチコマ」: R1解決が前提。esp-sr は依存導入済みだが
  カスタムウェイクワードはEspressifの学習が必要。R1後に別要件として起こす
- Funnel による実機の直接公開（R7完了まで）
