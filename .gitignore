.DS_Store
.idea/
__pycache__/
*.pyc
.env
# Forwarder settings: no credentials, but it names the private Tailscale
# address of the machine that holds every conversation. Ship the example.
.env.forwarder
# Copies merge_memory.py leaves behind; same personal data as the store.
*.before-merge
phase3-*.log
phase3-*.err.log
*.log
*.err.log
# Gateway conversation memory: personal data (name, location), never commit.
firmware/gateway/memory/
firmware/gateway/voicevox_dict.json
firmware/gateway/models/
