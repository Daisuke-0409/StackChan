# タチコマ設計思想 — 何が壊れても、これがあれば同じものが建つ

このファイルは大輔（一木大輔）の設計思想を明文化したもの。コードが全部
消えても、これと数枚の設計文書があれば、同じ骨格・同じ判断基準で作り直せる。
AI秘書テンプレート（PARAメソッド）から輸入した「蓄積の規律」と、タチコマ
開発で実戦から得た「安全と正しさの原則」を、一枚に統合する。

新しいセッションは、迷ったらまずここを読む。

---

## 0. これは誰のための、何か

タチコマ = 大輔専用の音声対話コンパニオン。M5Stack StackChan の頭脳を
自前の AI ゲートウェイに差し替えたもの。**製品ではなく、大輔の生活と
仕事に根を張る道具**。将来 KFC も墓所照会も注文もこなす「AI秘書」に育つ卵。

---

## 1. 不変の原則（設計判断が割れたら、ここに戻る）

### 1-1. 人格は1つ、入口は複数
記憶・権限・声・感情は1つ。実機・PC・メガネ・会社の体は「入口」でしかない。
体を増やしても人格は分裂させない。会社PCでゲートウェイを起動しないのは
この原則の帰結（頭が2つになると記憶が枝分かれする）。

### 1-2. デバイスに AI を載せない
頭は PC（将来 Mac mini）上のゲートウェイ1つ。API キーはデバイスに渡さない。
デバイスは録音して送り、返ってきた音声を再生するだけの「体」。

### 1-3. 秘密は「守らせる」のではなく「渡さない」
聞かせてはいけない事実は、モデルのコンテキストに載せない。
「言うな」とプロンプトで頼む設計は、同情的な聞き方で破られる。
- 顧客データを Gemini に渡さない（CRM照会は定型文組み立て、AI不要）
- master限定の記憶は、声紋でmasterと確認できるまでモデルに渡さない
- 音声そのものも家から出さない（ローカルSTT）
**これはコード構造で強制する。プロンプトの善意に依存しない。**

### 1-4. 推測しない。分からないは分からないと言う
- STTは聞き取れない語を勝手に埋めない（語彙1語が一番危険だった実例あり）
- 注文は正式名が無ければ聞き返す。AI推測時は「〜と解釈したよ」と必ず宣言
- CRM照会は名前が複数なら「6件あるよ、下の名前は?」と返す
- 決済直前でカートが想定と1円でも違えば止める

### 1-5. 金は動かせても、二重には動かさない
決済は「ミスが起きない」ではなく「ミスが起きても金が動かない/二度動かない」で
守る。ドライラン既定、上限額、承認ゲート、突合検証、**リトライ絶対禁止**、
結果不明時は人に回す。プリペイド（スタバカード・モスカード）を優先するのは
被害上限が物理的に決まるから。

### 1-6. 使えなくなるのは論外
既存の動作を壊す改修は、どんなに正しくても最後に回す。
リファクタは「純粋ロジックの抽出→再エクスポート→テスト不変」の順で、
振る舞いを変えずに進める。移行は1モジュールずつ。

---

## 2. 蓄積の規律（AI秘書テンプレートから輸入）

**目的: 同じミスを二度踏まない。半年後の自分（or 後任）が読んで動ける。**

### 2-1. 二層メモリ
- **Hot cache = CLAUDE.md** … 毎セッション読む。全体ルールと「今どこ・次何」
- **詳細 = docs/ と memory/** … 契約・手順・失敗パターンの全集

### 2-2. 失敗パターンは台帳に貯める
実際に踏んだ罠は CLAUDE.md「環境固有の落とし穴」に、症状→原因→対処で残す。
新しい学習が出たら、まずそこへ。頻出するものだけ本文の目立つ場所へ昇格。
（例: PS5.1のUTF-8誤読、DHCPでIPが変わり機体無言、タスク自動無効化）

### 2-3. 状態は1枚に集約
「今どこ・次何」は NEXT.ps1 が機械から生成する点検表を正とする。
人が書くのは CLAUDE.md「次セッションの開始点」の三部構成:
前回ここまで → 全体の中で現在ここ → 今回はここから。

### 2-4. セッションは必ずこの三部で開く（大輔の恒久指定）
毎セッション冒頭で「前回終わった所 / 全体の現在地 / 今回の開始点」を述べてから作業に入る。

---

## 3. 判断のデフォルト（迷ったときの倒し方）

| 状況 | 倒す方向 |
|---|---|
| 安全 vs 便利 | 安全（特に金・顧客データ） |
| 推測 vs 聞き返し | 聞き返し（データに近いほど） |
| 動く既存 vs 綺麗な新実装 | 動く既存を壊さない |
| 沈黙する失敗 vs 大声の失敗 | 大声で失敗させる（無言の劣化が最悪） |
| 機能追加 vs 監視・再現性 | 土台（監視・再現性）を先に |
| プロンプトで頼む vs 構造で強制 | 構造で強制 |

---

## 4. 開発のリズム

- 実機flash・git push・シークレットの扱いは、実行前に必ず人間に確認
- flash前に16MBフルバックアップ（16,777,216バイトちょうどを検証）
- 修正は「観測→仮説→修正→実機ログで検証」。勘で直さない
- 受け入れ条件を満たすまで完了と報告しない
- ドキュメントは実装と同じコミットで更新する
- モデルは適宜切り替えてトークン効率重視。ただし必要な所でケチらない

---

## 5. なぜ卵（AI秘書テンプレート）を取り込んだか

AI秘書テンプレートは実行プロセスを持たない「賢い秘書の習慣」。タチコマは
「堅牢なシステムの骨格」。前者の弱点（実体がない）は後者が埋め、後者の弱点
（運用・再現性の甘さ、2026-08-22の監査で判明）は前者の蓄積規律が埋める。
思想として優れるのはどちらでもなく、**噛み合わせると欠けが消える**。
この章2がその噛み合わせの記録。
